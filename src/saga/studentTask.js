import { ASYNC_INCREASE } from "../redux/action/counterAction";
import { take } from "redux-saga/effects"

export default function* () {
    // while (true) {
    // const result = yield take(ASYNC_INCREASE)
    // console.log("async increase action发生了", result);
    // }

}